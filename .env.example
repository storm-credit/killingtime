# Copy to .env and fill in only the keys you need. Not checked into git.
# Selecting engine=local (default) requires no API key at all.

# Claude (engine=claude)
ANTHROPIC_API_KEY=

# Gemini (engine=gemini)
GEMINI_API_KEY=

# GPT (engine=gpt)
OPENAI_API_KEY=
